// import { useState } from "react";

export default function CityList({ cities }) {
    return (
        <h2>Cities</h2>
        // <ul>
        //     {cities.map(city => (
        //         <li key = {city.name}>
                    
        //         </li>
        //     ))}
        // </ul>
    );
}
